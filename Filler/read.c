
#include "libft/get_next_line.h"
#include <stdio.h>
#include <unistd.h>

int		main(void)
{
	//char	*line;

	//while (get_next_line(0, &line))
	//	printf("%s\n", line);
	write (1, "3x3\n", 5);
	return (0);
}